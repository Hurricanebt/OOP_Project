import * as t from '../../actionTypes/newsListActionsTypes';

describe('user actions', () => {

    it('REFRESH_USER', () => {
        const action = {
            type: t.REFRESH_USER
        }

        
    });

})
