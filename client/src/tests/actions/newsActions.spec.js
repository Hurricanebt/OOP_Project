import * as t from '../../actionTypes/newsActionsTypes';

describe('user actions', () => {

    it('REFRESH_USER', () => {
        const action = {
            type: t.REFRESH_USER
        }

        
    });

})
